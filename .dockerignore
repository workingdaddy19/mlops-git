# ── Python 캐시 ──────────────────────────────────────────────
.venv/
__pycache__/
*.pyc
*.pyo
*.pyd

# ── 환경 설정 (k8s Secret으로 주입) ─────────────────────────
.env
.env.*
!.env.example

# ── 로그/임시 ─────────────────────────────────────────────────
*.log
*.tmp

# ── VCS / 개발 도구 ───────────────────────────────────────────
.git/
.gitignore
.claude/
.bkit/
.streamlit/

# ── 개발 전용 파일 ────────────────────────────────────────────
docs/
libs/
demo/
docker/
docker-compose*.yml

# ── 인프라 구성 (이미지 미포함 — 인프라팀이 kubectl로 적용, 실 시크릿 포함) ──
k8s/

# ── 빌드/배포 운영 스크립트 (빌드 호스트 전용, 런타임 이미지 불필요) ──
build_and_push*.sh
deploy_eks*.sh
start_backend.bat

# ── 아카이브 (서버 배포 불필요한 개발 흔적·미사용 소스) ──
archive/

# ── 불필요 파일 ───────────────────────────────────────────────
alembic.ini
dbcreate.md
booterr_*.log
boot_final.log
README*.md
