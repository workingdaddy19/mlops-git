name: iris-mlops-demo

conda_env: conda.yaml

entry_points:
  train:
    parameters:
      algorithm: {type: string, default: "RandomForest"}
      n_estimators: {type: int, default: 100}
      max_depth: {type: int, default: 5}
    command: "python mlops_demo.py"
