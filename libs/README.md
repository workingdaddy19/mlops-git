# libs 디렉토리
# 이 폴더에 외부 JAR 파일을 배치하세요:
#   - Initech SSO JAR   (ex: initech-eam-*.jar)
#   - MarkAny DRM JAR   (ex: markany-drm-*.jar)
#
# build.gradle 에서 다음과 같이 참조됩니다:
#   implementation fileTree(dir: 'libs', include: ['*.jar'])
#
# JAR가 없으면 이 폴더는 비워두어도 빌드에 영향 없습니다.
